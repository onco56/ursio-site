<section class="two-column">
  <div class="col left">
    <slot name="left" />
  </div>

  <div class="col right">
    <slot name="right" />
  </div>
</section>

<style>
  .two-column {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-5);
  }

  .col {
    flex: 1;
  }
</style>

